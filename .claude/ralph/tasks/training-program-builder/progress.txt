# Training Program Builder - Progress Log

## Codebase Patterns

*This section will be updated as patterns are discovered during implementation.*

---

## Ready to Start

Ralph autonomous agent is ready to begin implementing user stories.
Run the Ralph agent to start execution.

---

## Codebase Patterns

- JSON imports in TypeScript tests require casting: `const items = jsonData as MyType[]` since TypeScript infers string types from JSON, not literal union types
- Exercise images are stored in `/images/` directory with filenames like `00251201-Barbell-Bench-Press_Chest_thumbnail@3x.jpg`
- Types are exported from `/types/` directory (e.g., `types/exercise.ts`)
- Static data lives in `/data/` directory with tests in `/data/__tests__/`

---

## 2026-03-18 - US-001

- Created `/types/exercise.ts` with `Exercise` interface and `MuscleGroup` union type
- Created `/data/exercises.json` with 30 exercises covering all major muscle groups
- Created `/data/__tests__/exercises.test.ts` with 8 tests validating JSON structure
- All exercises mapped to existing image filenames in `/images/` directory
- **Learnings for future iterations:**
  - TypeScript infers JSON imports as `string` for union type fields — cast with `as Exercise[]` in tests
  - Image filenames follow pattern: `{id}-{Exercise-Name}_{MuscleGroup}_thumbnail@3x.jpg`
  - The `/images/` directory has hundreds of exercises available for future expansion
---

## 2026-03-18 - US-002

- Installed `react-native-mmkv` (v3, uses `createMMKV()` factory)
- Created `/types/program.ts` with TypeScript interfaces: `Program`, `Session`, `ExerciseConfig`, `Set`, `DayKey`
- Created `/utils/storage/programs.ts` with all storage functions: `saveProgram`, `getProgram`, `getAllPrograms`, `deleteProgram`, `saveDraft`, `getDraft`, `clearDraft`, `setActiveProgram`, `getActiveProgram`
- Created `/__mocks__/react-native-mmkv.js` — in-memory MMKV mock with `clearAllMMKVInstances()` helper
- Created `/__mocks__/react-native-mmkv.d.ts` — module augmentation to add `clearAllMMKVInstances` to the types without overriding the real exports
- Created `/utils/storage/__tests__/programs.test.ts` with 12 tests covering all storage functions
- **Learnings for future iterations:**
  - `react-native-mmkv` v3 uses `createMMKV({ id })` (not `new MMKV()`), and `remove(key)` (not `delete(key)`)
  - MMKV instance is module-level in the storage file, so tests share state — call `clearAllMMKVInstances()` in `beforeEach`
  - Use `import 'react-native-mmkv'; declare module 'react-native-mmkv' { ... }` to augment existing package types without replacing them
---

## 2026-03-18 - US-003

- Updated `/app/(tabs)/entrainement.tsx` to load programs from MMKV using `getAllPrograms()` with `useEffect`
- Displays program list (titles) when programs exist, empty state "Aucun programme créé" when none
- Created `/app/(tabs)/__tests__/entrainement.test.tsx` with 6 render tests
- Created `/__mocks__/react-native-safe-area-context.js` manual mock for component tests
- Updated `jest.config.js` to use `jest-expo` preset (replaces `jsdom` which broke React Native native bridge)
- **Learnings for future iterations:**
  - NativeWind is NOT installed in this project — use `StyleSheet.create()` despite CLAUDE.md rule
  - `jsdom` test environment breaks React Native component tests due to missing native bridge; use `jest-expo` preset
  - React Native component tests need mocks for: `react-native-safe-area-context`, `expo-router`, `@expo/vector-icons`
  - Create `/__mocks__/react-native-safe-area-context.js` as a manual mock file (inline jest.mock factory can't reference out-of-scope variables like `React`)
  - `jest-expo` preset handles `__DEV__`, native module setup, and transformIgnorePatterns automatically
---

## 2026-03-18 - US-004

- Created `/app/program/new.tsx` route with header and back button
- Created `/app/program/__tests__/new.test.tsx` with 3 tests (render title, back button, navigation)
- Updated `/app/(tabs)/__tests__/entrainement.test.tsx` to add navigation test (7 tests total)
- **Learnings for future iterations:**
  - `jest.requireMock('module')` must be used instead of `require()` to access mock internals — `require()` triggers ESLint `@typescript-eslint/no-require-imports` warning
  - Variables defined before `jest.mock()` are undefined at mock factory evaluation time due to hoisting — use inline `jest.fn()` in factory + `jest.requireMock()` in tests
  - App routing uses Expo Router file-based routing: `/app/program/new.tsx` → route `/program/new`
---

## 2026-03-18 - US-005

- Expanded `/app/program/new.tsx` with full layout: title input, description input, 7 day blocks, save button, modifier button
- Updated `/app/program/__tests__/new.test.tsx` with 10 tests covering all new UI elements
- Removed unused `eslint-disable-next-line` directives in test files (lint warning cleanup)
- **Learnings for future iterations:**
  - `eslint-disable-next-line @typescript-eslint/no-var-requires` is no longer needed for `jest.requireMock()` — ESLint doesn't flag it as `require()`
  - ScrollView with `contentContainerStyle` needed for scrollable page content in React Native
  - Day blocks use `testID={`day-block-${day}`}` pattern for targeted test assertions
---

## 2026-03-18 - US-006

- Updated `/app/program/new.tsx` with auto-save draft using debounced 2s timer
- Load draft on mount via `getDraft()`, populate title/description state
- Clear draft when save button pressed via `clearDraft()`
- Visual indicator "Brouillon sauvegardé" shown for 2s after each auto-save
- Added mock for `utils/storage/programs` in test file
- Updated tests with `jest.useFakeTimers()` for 8 new tests (17 total)
- **Learnings for future iterations:**
  - `jest.useFakeTimers()` in `beforeEach` + `jest.useRealTimers()` in `afterEach` is the correct pattern for timer tests
  - Wrap `jest.advanceTimersByTime()` in `act(() => {...})` so React processes state updates triggered by timers
  - `isMountRef` with `useRef(true)` set to `false` in mount effect doesn't prevent auto-save on initial mount — the second effect runs AFTER first, so isMountRef is already false. This is OK since initial timer gets cleared by first user interaction.
  - Mock storage module per-test file: `jest.mock('../../../utils/storage/programs', () => ({ saveDraft: jest.fn(), getDraft: jest.fn(() => null), clearDraft: jest.fn() }))`
---

## 2026-03-18 - US-007

- Updated `/app/program/new.tsx` to navigate to `/program/session/new` with `{ day }` param on pressing "+ Ajouter une séance"
- Created `/app/program/session/new.tsx` — session page with header "Modifier la séance" and back button
- Created `/app/program/session/__tests__/new.test.tsx` with 5 tests (render, back nav, day param display)
- Added navigation test to `/app/program/__tests__/new.test.tsx` (18 tests total)
- **Learnings for future iterations:**
  - Expo Router navigation with params: `router.push({ pathname: '/program/session/new', params: { day } })`
  - Read route params with `useLocalSearchParams<{ day: string }>()` from `expo-router`
  - Mock `useLocalSearchParams` in tests: `jest.mock('expo-router', () => ({ router: { ... }, useLocalSearchParams: jest.fn(() => ({ day: 'LUN' })) }))`
  - To test different params, call `useLocalSearchParams.mockReturnValue(...)` via `jest.requireMock()`
---

## 2026-03-18 - US-008

- Expanded `/app/program/session/new.tsx` with full edit session layout
- Added: editable title input (default "Upper A"), editable description input (default "Description")
- Added: empty exercise list area with placeholder text
- Added: "+ Ajouter des exercices" button at bottom with blue border
- Added: checkmark validate button (top right) that calls router.back()
- Updated `/app/program/session/__tests__/new.test.tsx` from 5 to 13 tests
- **Learnings for future iterations:**
  - Session page uses ScrollView with footer outside it (for fixed bottom button pattern)
  - `fireEvent.changeText(input, value)` tests controlled TextInput; check `input.props.value`
  - Validate checkmark button uses `testID="validate-button"` for test targeting
---

## 2026-03-18 - US-009

- Updated `/app/program/session/new.tsx`: on validate, creates Session object, appends to draft `days[day]`, saves draft, then navigates back
- Updated `/app/program/new.tsx`: added `days` state, `useFocusEffect` to reload sessions on focus, session cards in day blocks
- Added session card styles (title + exercise count) with navigation to session edit page
- Updated `/app/program/__tests__/new.test.tsx`: changed `useFocusEffect` mock to use `React.useEffect(cb, [])`, added 4 session display tests (21 total)
- Updated `/app/program/session/__tests__/new.test.tsx`: added storage mock, reset `useLocalSearchParams` in `beforeEach`, added 4 session save tests (17 total)
- **Learnings for future iterations:**
  - `useFocusEffect` mock: use `React.useEffect(cb, [])` inside mock, NOT `cb()` directly — direct call runs on every render and causes infinite loop when it sets state
  - `jest.clearAllMocks()` does NOT reset `mockReturnValue` — if a test calls `mockReturnValue(someValue)`, subsequent tests see that value unless overridden in `beforeEach`
  - Data flow between pages: use MMKV draft as shared state; session page writes to draft on validate, program page reads from draft via `useFocusEffect`
---
